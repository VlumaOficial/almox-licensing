// Centralização de configurações - Produção (TypeScript)
export { supabaseConfig } from './supabase.js';

// Configurações adicionais podem ser adicionadas aqui
export const appConfig = {
  name: 'AlmoxPro Licensing',
  version: '1.0.0',
  environment: 'production'
};
